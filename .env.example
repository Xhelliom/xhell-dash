# Exemple de configuration d'environnement pour Xhell-Dash
# Copiez ce fichier en `.env` ou `.env.local` et adaptez les valeurs.

#######################################
# Base de données (Prisma / SQLite)
#######################################

# SQLite local (fichier dev.db dans le dossier prisma)
DATABASE_URL="file:./prisma/dev.db"

#######################################
# Auth.js / NextAuth
#######################################

# Secret cryptographique utilisé par Auth.js pour signer les cookies / tokens.
# Générez une valeur robuste (32+ caractères aléatoires) :
#   - sous Linux/macOS : openssl rand -base64 32
#   - sous Windows PowerShell : [Convert]::ToBase64String((1..32 | ForEach-Object {Get-Random -Maximum 256}))
AUTH_SECRET="remplace-moi-par-un-secret-solide"

# URL publique de l'application. Obligatoire en prod (derrière un proxy ou en Docker).
# En dev local, vous pouvez laisser la valeur par défaut si vous utilisez le port 3000.
AUTH_URL="http://localhost:3000"

#######################################
# Notes sur les comptes utilisateurs
#######################################

# Au premier démarrage (et tant qu'aucun admin n'existe en base), un utilisateur
# admin sera automatiquement créé avec les identifiants suivants :
#   - login (email) : xhell-admin@example.com
#   - mot de passe  : Admin123!
#
# Pensez à :
#   - changer ce mot de passe dès la première connexion,
#   - créer vos propres comptes via un outil dédié (formulaire d'admin, script Prisma, etc.).



#######################################
# Chiffrement des tokens sensibles
#######################################

# Clé de chiffrement AES-256-GCM (32 bytes) pour chiffrer les tokens API dans la base de données.
# Les tokens sensibles (plexToken, apiKey, etc.) seront automatiquement chiffrés lors de leur stockage.
# 
# Générer une clé :
#   - sous Linux/macOS : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#   - sous Windows PowerShell : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 
# Format accepté :
#   - Hexadécimal : 64 caractères (recommandé)
#   - Base64 : 44 caractères
# 
# Si cette variable n'est pas définie, les tokens seront stockés en clair (avec un avertissement).
# Pour plus d'informations, consultez lib/SECURITY.md
ENCRYPTION_KEY=""

#######################################
# Variables d'environnement pour les tokens API (optionnel)
#######################################

# Au lieu de stocker les tokens directement dans apps.json, vous pouvez utiliser des variables
# d'environnement. Dans apps.json, utilisez le format : "${VAR_NAME}"
# 
# Exemple dans apps.json :
#   "plexToken": "${PLEX_TOKEN}"
# 
# Puis définissez la variable ici :
# PLEX_TOKEN="your-actual-plex-token-here"
#
# Vous pouvez aussi utiliser des valeurs par défaut : "${VAR_NAME:default-value}"
#
# Variables courantes (décommentez et remplissez selon vos besoins) :
# PLEX_TOKEN=""
# SONARR_API_KEY=""
# RADARR_API_KEY=""
# LIDARR_API_KEY=""
# TRUENAS_API_KEY=""
# HOMEASSISTANT_API_KEY=""
# PROXMOX_API_KEY=""
# KUBERNETES_TOKEN=""

#######################################
# Widget Weather (optionnel)
#######################################

# Clé API OpenWeatherMap pour le widget météo
# NEXT_PUBLIC_WEATHER_API_KEY="your-openweathermap-api-key-here"
